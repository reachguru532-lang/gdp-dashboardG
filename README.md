Commander Guru E-Mart — Deploy Package\n\nWhat's included:\n- app.py (main Streamlit app)\n- products.csv (editable product list)\n- images/ (product images)\n- requirements.txt (includes optional gspread libs)\n- .streamlit/config.toml (theme)\n- README.md (this file)\n\nQuick run locally:\n1. Unzip this package into a folder.\n2. (Optional) create a virtualenv: python -m venv venv && source venv/bin/activate\n3. Install dependencies:\n   pip install -r requirements.txt\n4. Run the app:\n   streamlit run app.py\n5. Open http://localhost:8501\n\nDeploy to Streamlit Community Cloud (recommended):\n1. Create a GitHub repo and push the entire folder.\n   git init\n   git add .\n   git commit -m \"Initial commit - Commander Guru E-Mart\"\n   git branch -M main\n   git remote add origin https://github.com/<your-username>/commander-guru-e-mart.git\n   git push -u origin main\n2. Go to https://share.streamlit.io and connect your GitHub account.\n3. Select the repo and branch (main), then deploy.\n\nOptional: Google Sheets order logging (recommended)\n- Create a Google Cloud service account and download the JSON key.\n- Share a Google Sheet with the service account email (edit access).\n- Install gspread and oauth2client (already in requirements.txt).\n- Add the logging code snippet in README or app.py as described previously.\n\nSecurity note:\n- Never commit service account JSON to a public GitHub repo. Use Streamlit secrets or environment variables for credentials when deploying.\n